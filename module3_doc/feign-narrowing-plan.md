---

Plan

1. Confirm scope boundary before editing. Only touch the two catch blocks (and their surrounding try/catch structure) in placeOrder() and viewAllProducts() in OrderServiceImpl.java. Do not touch the FeignException.NotFound handling logic itself (already correct/separately tracked as dead-code-suspect per iteration-log.md), and do not introduce a new custom exception hierarchy or @ControllerAdvice — keep the fix localized, consistent with the existing pattern of catching in the service layer and throwing ResponseStatusException.
2. Add a logger to OrderServiceImpl. Introduce an SLF4J org.slf4j.Logger/LoggerFactory field (private static final Logger logger = LoggerFactory.getLogger(OrderServiceImpl.class);) at the top of the class. This is a new pattern for the codebase (none currently exists), so use plain SLF4J (already on the classpath transitively via Spring Boot) rather than adding a new logging dependency.
3. In placeOrder(int productId), add exception narrowing after the existing catch (FeignException.NotFound ex) block:
   - Add catch (RetryableException ex) (import feign.RetryableException) before the generic FeignException catch, for the true "service unreachable" case (connection refused, timeout, DNS failure — no HTTP response received). Log at WARN level (this is an operational/environmental condition, expected to self-resolve, not a bug) including: the downstream call ("IProductServiceFeignClient#getById"), the productId, and ex.getMessage(). Then throw ResponseStatusException(HttpStatus.SERVICE_UNAVAILABLE, "Product service is currently unreachable, please try again later") — this is the only branch that should legitimately produce the "unreachable" 503 message.
   - Add a catch (FeignException ex) block (retained, now only reached for real non-404 HTTP responses — 4xx other than 404, or 5xx from the downstream service) that logs at ERROR level (a genuine downstream failure/misbehavior, worth surfacing loudly) including: the downstream call, productId, ex.status() (the actual HTTP status code returned), and ex.getMessage(). Do not log request/response bodies or headers (avoid leaking any sensitive data such as auth tokens). Then throw a ResponseStatusException whose status reflects the real cause rather than always 503 — map it as: if ex.status() is in the 5xx range, use HttpStatus.BAD_GATEWAY (502) with a message like "Product service returned an error, please try again later"; if ex.status() is in the 4xx range (excluding 404, already handled), use HttpStatus.INTERNAL_SERVER_ERROR (500) with a generic message like "Unable to process request due to an internal error" (do not echo the downstream 4xx reason back to the order-service caller, since a 400/401 from the internal Feign call is an order-service bug, not something the caller can act on).
4. In viewAllProducts(), apply the same two-catch-block narrowing (there is no NotFound case here since it returns a list, not a single resource): add catch (RetryableException ex) → WARN log + 503 "unreachable" ResponseStatusException, then catch (FeignException ex) → ERROR log (call name "IProductServiceFeignClient#getAllProducts", ex.status(), ex.getMessage()) + ResponseStatusException mapped by status range exactly as in step 3 (502 for 5xx, 500 for other 4xx).
5. Add the RetryableException import (feign.RetryableException) to OrderServiceImpl.java; the existing feign.FeignException and Spring imports stay as-is.
6. Update OrderServiceImplTest.java:
   - Modify placeOrder_throwsServiceUnavailable_whenProductServiceUnreachable (or rename it, e.g. placeOrder_throwsBadGateway_whenProductServiceReturns500) — since generalFeignException() builds a 500 via FeignException.errorStatus(...), its expected status must change from HttpStatus.SERVICE_UNAVAILABLE to HttpStatus.BAD_GATEWAY, reflecting the new mapping in step 3.
   - Add a new test, e.g. placeOrder_throwsServiceUnavailable_whenProductServiceUnreachable, that mocks feignClient.getById(...) to throw an actual feign.RetryableException (construct via its public constructor, e.g. new RetryableException(-1, "connection timed out", Request.HttpMethod.GET, (Long) null, request) — check the exact constructor signature available in the Feign version on the classpath before writing) and asserts the result is ResponseStatusException with HttpStatus.SERVICE_UNAVAILABLE.
   - Add a new test asserting a non-404, non-5xx FeignException (e.g. a 400 built via the existing errorStatus(400, ...) helper) maps to HttpStatus.INTERNAL_SERVER_ERROR, not 503 — this is the concrete "no longer mislabeled as unreachable" regression test called for in the task.
   - Add equivalent three-case coverage (real unreachable, 5xx, other 4xx) for viewAllProducts(), which currently has no dedicated test coverage at all in this file — confirm by searching the test file for viewAllProducts before assuming none exists.
7. Record the change as a memory decision. Since decision-001.md documented the original catch-all-to-503 pattern this fix replaces, add a new decision entry (e.g. decision-004.md) in .memory/project/decisions/ summarizing: what changed (narrowed FeignException handling, added RetryableException-specific branch, added logging, added new status mappings), why (catch-all was mislabeling all non-404 failures as "unreachable" and swallowing the real cause), and update .memory/project/MEMORY_INDEX.md to reference it rather than leaving decision-001.md as the only record of this area. This step is the Implementer's responsibility since it requires file_write, which the Planner does not have.

---

File List (in order of change)

1. /workspace/ecom-order-service/src/main/java/com/productorder/service/OrderServiceImpl.java — add logger field; rework placeOrder()'s catch-all block (currently line 32) into RetryableException + narrowed FeignException blocks with status-based mapping and logging; rework viewAllProducts()'s catch-all block (currently line 40) the same way; add feign.RetryableException import.
2. /workspace/ecom-order-service/src/test/java/com/productorder/service/OrderServiceImplTest.java — update the existing 500-case test's expected status (currently asserts SERVICE_UNAVAILABLE at the placeOrder_throwsServiceUnavailable_whenProductServiceUnreachable test, ~lines 51-58); add a new RetryableException-based test for the true-unreachable case; add a new non-404/non-5xx (400) case test; add viewAllProducts() test coverage (success, 5xx, 4xx, and unreachable cases) since none currently exists — confirm this last point in the file before writing new tests.
3. /workspace/.memory/project/decisions/decision-004.md (new file) — record the decision superseding/narrowing decision-001.md's catch-all pattern.
4. /workspace/.memory/project/MEMORY_INDEX.md — add/update the entry pointing to the new decision file.

Note to Implementer: Verify the exact feign.RetryableException constructor signature available on this project's Feign version (via codebase_search on the local Maven dependency jars/sources or the pom.xml Feign version) before writing the test in step 6, since its constructor signature has changed across Feign versions. Also verify with ./mvnw test per the pattern established in decision-001.md's follow-up and decision-003.md, noting in the decision record if the build cannot be run due to the offline-Maven issue previously documented.
